h1 Transaction
form(action="/transaction/create", method="POST")
    select(name="username")
        each user in users
            option= user.name
    select(name="booksname")
        each book in books
            option= book.name 
    button Create